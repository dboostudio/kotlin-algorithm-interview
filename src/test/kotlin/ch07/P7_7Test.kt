package ch07

import io.kotest.core.spec.style.ShouldSpec

class P7_7Test : ShouldSpec({

    val P7_7 = P7_7()

    context("twoSum") {
        // 두수의 합이 target임을 만족하는 index 2개를 리턴한다.
        should("return 2 indexes which sum equals to target: twoSum1") {
            
        }
        should("return 2 indexes which sum equals to target: twoSum2") {

        }
    }
})
